div.form-element
  label(for="temporal") temporal
  input(type="text", id="temporal", name="temporal", placeholder="temporal")
  div.description The range of temporal applicability of a dataset, e.g. for a 2011 census dataset, the year 2011 (in ISO 8601 time interval format).