div.form-element
  label(for="predecessorOf") predecessorOf
  input(type="text", id="predecessorOf", name="predecessorOf", placeholder="predecessorOf")
  div.description A pointer from a previous, often discontinued variant of the product to its newer variant.