div.form-element
  label(for="defaultValue") defaultValue
  input(type="text", id="defaultValue", name="defaultValue", placeholder="defaultValue")
  div.description The default value of the input.  For properties that expect a literal, the default is a literal value, for properties that expect an object, it&#39;s an ID reference to one of the current values.