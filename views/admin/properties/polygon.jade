div.form-element
  label(for="polygon") polygon
  input(type="text", id="polygon", name="polygon", placeholder="polygon")
  div.description A polygon is the area enclosed by a point-to-point path for which the starting and ending points are the same. A polygon is expressed as a series of four or more space delimited points where the first and final points are identical.