div.form-element
  label(for="siblings") siblings
  input(type="text", id="siblings", name="siblings", placeholder="siblings")
  div.description A sibling of the person (legacy spelling; see singular form, sibling).