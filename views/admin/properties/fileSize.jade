div.form-element
  label(for="fileSize") fileSize
  input(type="text", id="fileSize", name="fileSize", placeholder="fileSize")
  div.description Size of the application &#x2F; package (e.g. 18MB). In the absence of a unit (MB, KB etc.), KB will be assumed.